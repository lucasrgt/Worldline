package worldline.api;

/** Optional operations exposed by a semantic game UI adapter. */
public enum GameUiCapability {
    SEMANTIC_TREE,
    INVENTORY_LIFECYCLE,
    NODE_CLICK,
    KEYBOARD,
    POINTER,
    FOCUS,
    DRAG_DROP,
    GEOMETRY,
    SCREENSHOT
}
