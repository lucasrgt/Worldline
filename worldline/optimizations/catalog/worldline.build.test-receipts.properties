schema=worldline.optimization.v1
id=worldline.build.test-receipts
summary=Reuse exact test compilation artifacts and PASS receipts while running misses in bounded parallel workers.
subsystem=verification.tests
status=active
default.enabled=true
behavior.delta=Changes test latency only; platform, suite, module, and dependency input drift executes the affected suite.
risks=An incomplete suite fingerprint could mask a platform-specific failure.
rollback=Disable TestReceiptCache restoration and compile and execute every selected suite on each Gate.
tracking=symbol
source.symbols=TestBuild#compileAndRun,TestBuild#runSuites
evidence=tools/harness/TestReceiptCacheTest#main;reports/verify.json
