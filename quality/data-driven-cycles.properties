# Data-driven smoke-cycle adoption ratchet.
minimum.generic.milestones=386
# M620 and M767 own external loader builds, provider assembly, and paired process lifecycles;
# M768 owns restored worlds, eight fresh client processes, and complete-frame census sealing;
# M769 joins a complete-frame WLPR census to a same-process JFR timeline;
# M770 coordinates three fresh GPU-query and present-attribution client arms;
# M771 coordinates eight fresh counterbalanced negative-control client arms;
# M772 owns fresh-world autosave arms with persistence reloads; M773 owns
# eight counterbalanced real-client chunk-scheduler arms plus a saved template;
# M774 owns an external TestKit consumer; and M775 owns twelve counterbalanced
# GPU-client arms plus a restored 576-machine template. M779 owns four fresh
# ABBA GPU clients, full-frame differential capture, and a pinned Aero build;
# M780 owns a pinned Aero build, restored world, and four fresh GPU clients for
# resolved-light value equivalence plus cache-policy classification; M781 owns
# a pinned Aero build and six fresh JVMs for startup policy and world lifecycle;
# M782 owns a pinned StationAPI build and two 256-epoch Java 8 scheduler soaks;
# M783 owns eight fresh GPU clients and bounded visible-latency classification;
# M784 owns eight counterbalanced GPU clients, a restored four-chunk fixture,
# and full-RGBA Cell Page equivalence plus preset classification.
# The entity physical-envelope census owns public TestKit plan expansion; M785
# and M786 own external SDK compilation, discovery, and compatibility probes.
maximum.exceptional.coordinators=102
