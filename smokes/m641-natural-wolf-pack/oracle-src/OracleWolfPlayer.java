/** Minimal official observer for the peaceful-spawn radius. */
final class OracleWolfPlayer extends em {
  OracleWolfPlayer(dj world, int x, int z) {
    super(world);
    c(x + 0.5D, 66D, z + 0.5D, 0F, 0F);
  }
}
