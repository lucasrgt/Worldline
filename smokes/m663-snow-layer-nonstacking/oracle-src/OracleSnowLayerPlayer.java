/** Minimal official observer that activates the native ambient scheduler. */
final class OracleSnowLayerPlayer extends em {
    OracleSnowLayerPlayer(dj world, int x, int z) {
        super(world);
        c(x + 0.5D, 66D, z + 0.5D, 0F, 0F);
    }
}
