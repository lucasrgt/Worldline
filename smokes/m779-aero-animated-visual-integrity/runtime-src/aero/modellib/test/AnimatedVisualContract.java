package aero.modellib.test;

/** Package bridge that keeps the fixture independent of Worldline internals. */
public final class AnimatedVisualContract {
    public static final int ROUTE_FRAMES = 240;
    private AnimatedVisualContract() {}
}
