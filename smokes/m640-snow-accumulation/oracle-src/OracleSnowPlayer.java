/** Minimal official observer for the active-chunk scheduler. */
final class OracleSnowPlayer extends em {
  OracleSnowPlayer(dj world, int x, int z) {
    super(world);
    c(x + 0.5D, 66D, z + 0.5D, 0F, 0F);
  }
}
