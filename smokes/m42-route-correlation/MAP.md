# M42 Caller-Owned Route Correlation

Two fresh sessions pass one opaque caller-owned object through a safe primary.
Its synchronous event and the terminal execution summary must retain that
exact object by identity.

Worldline does not inspect, serialize, compare, or register the correlation.
The controller stops after `0:0:PRIMARY`, leaving the later alternative
absent. Cache remains coherent and official player NBT persists the accepted
pose.
