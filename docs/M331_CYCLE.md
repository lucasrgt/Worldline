# M331 qualification cycle

`ThrowablesSetCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run air-uses snowball item `332`, egg item `344`, and
fishing rod item `346`, then correlates Packet23 types `61`, `62`, and `90`
across two peers on the existing object tracker. The frozen signal must
include at least two of those three object types. One official EOF is
retried after a 5 second sleep.

The frozen semantic SHA-256 is
`63d18b0a65f745ad18fa9a7a9e8e345e8bffe83e067224ff8687c1b03c0a7328`.

Run directly with:

```text
java tools/smoke/ThrowablesSetCycle.java m331-throwables-set
```

Canonical evidence uses two official server JVMs and four client sessions.
Headless protocol-14 only. No GUI. No Aero.
