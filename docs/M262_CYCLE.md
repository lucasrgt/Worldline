# M262 qualification cycle

`CookieEatCycle` rebuilds the cookie `357` fixture in two fresh official server
JVMs. Each run air-uses one cookie, heals Packet8 `19 -> 20`, consumes the
stack, and reloads that inventory plus health.

The frozen semantic SHA-256 is
`2374d08bdc38c0697e31d3009c028cbb6f70fb2794e2d20606804aaa4dbfb0bc`.

Canonical evidence uses two official server JVMs and four client sessions.
