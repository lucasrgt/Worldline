# M201 qualification cycle

`RedMushroomCycle` rebuilds the dark dirt pocket in two fresh official
server JVMs. Each run places red mushroom item `40` on dirt and reloads
block `40:0`.

The frozen semantic SHA-256 is
`26885845d6ad7c99bd324b497cb5415fe9056a79b295d940bc26da6049f0848a`.

Canonical evidence uses two official server JVMs and four client sessions.
