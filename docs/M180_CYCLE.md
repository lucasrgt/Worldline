# M180 qualification cycle

`FishingRodCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run casts fishing rod item `346` and correlates Packet23
type `90` across two peers.

The frozen semantic SHA-256 is
`9eafaf3ce4f443aa63f94304c269f66ab3a4257921e9d595d17a208ef9c8554a`.

Canonical evidence uses two official server JVMs and four client sessions.
