# M200 qualification cycle

`BrownMushroomCycle` rebuilds the raised dirt pocket in two fresh official
server JVMs. Each run places brown mushroom item `39` in the dark cell and
reloads block `39:0`.

The frozen semantic SHA-256 is
`4140e189cc2fa3f53a899ffa1b8332f24d0bf2a320fc2dc5f0050e9026718c70`.

Canonical evidence uses two official server JVMs and four client sessions.
