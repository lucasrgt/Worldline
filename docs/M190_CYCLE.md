# M190 qualification cycle

`JackOLanternCycle` rebuilds the raised stone fixture in two fresh
official server JVMs. Each run places jack-o-lantern item `91` with look
yaw `-90` and reloads facing metadata `1`.

The frozen semantic SHA-256 is
`6d925e40f9f78a804de2b69ee8eb5107d5314dedb78a27dd72a2acb8fd53f77d`.

Canonical evidence uses two official server JVMs and four client sessions.
