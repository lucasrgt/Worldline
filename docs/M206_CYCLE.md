# M206 qualification cycle

`SpongeCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places sponge item `19` on the top face and reloads
block `19:0`.

The frozen semantic SHA-256 is
`f403158d252cd74d08246cef3eee7b0ea15f96a1c88448ec6ce62d608093a441`.

Canonical evidence uses two official server JVMs and four client sessions.
