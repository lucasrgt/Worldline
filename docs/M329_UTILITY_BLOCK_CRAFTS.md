# M329 utility-block crafts

M329 opens the official workbench-craft boundary for fence `85`, ladder
`65`, and bookshelf `47` in one isolated compound cycle.

The actor places workbench `58` on the raised-stone fixture, opens that
window, and completes the vanilla recipes: six sticks to two fences, seven
sticks to two ladders, and six planks plus three books to one bookshelf.
Packet102 take of each result slot is accepted only when the official
server returns those exact IDs. The three stacks survive a clean save plus
fresh login.

Frozen semantic SHA-256:
`b70015b8e4bea597b4b8eeba287d216244d5c1bb9f83a1d7d06120bdb8c5086f`.

This milestone does not claim M173 fence collision, M174 ladder facing,
M189 bookshelf placement, or any other workbench recipe.
