# M199 qualification cycle

`RoseCycle` rebuilds the raised dirt fixture in two fresh official server
JVMs. Each run places rose item `38` on dirt `3` after stone rejection and
reloads block `38:0`.

The frozen semantic SHA-256 is
`d79a60342cee16cfece9348ecc6371263bd13bc5f50896d336b8fae6d9d750dd`.

Canonical evidence uses two official server JVMs and four client sessions.
