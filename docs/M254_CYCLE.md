# M254 qualification cycle

`WaterPlaceCycle` rebuilds the raised stone basin in two fresh official
server JVMs. Each run places water bucket `326` into empty air above the
support and reloads still water `9:0` plus empty bucket `325:1:0`.

The frozen semantic SHA-256 is
`7349aeca365432ce5e3996f11aa401973978fa83d7fe895578999c7cd306cac2`.

Canonical evidence uses two official server JVMs and four client sessions.
