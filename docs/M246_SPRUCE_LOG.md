# M246 spruce log

M246 opens the official spruce-log placement boundary. Packet15 of
wood/log item `17` with damage `1` on a raised stone support places
block `17:1`. That exact spruce cell remains after a clean save plus
fresh login.

This milestone is distinct from M208 oak log `17:0`. It does not claim
birch `17:2` or later axis data values.
