# M286 cyan wool

M286 opens the official cyan-wool placement boundary. Packet15 of wool
item `35` with damage `9` on a raised stone support places block `35:9`.
That exact cell remains after a clean save plus fresh login.

This milestone is distinct from light-blue wool `35:3` and blue wool
`35:11`. It does not claim other dye colors, sheep, or fire consumption.
Headless `B173WireClient` only. No GUI. No Aero.
