# M230 qualification cycle

`LapisOreCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places lapis ore item `21` on the top face and
reloads block `21:0`.

The frozen semantic SHA-256 is
`f684efb5f52db98a991a7edd381072bb58cc997a3d3bfd07327725c2e5026139`.

Canonical evidence uses two official server JVMs and four client sessions.
