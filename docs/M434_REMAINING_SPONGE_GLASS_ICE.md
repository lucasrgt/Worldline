# M434 remaining sponge glass ice

M434 opens the official remaining transparent/odd-solid place set.
Packet15 places sponge `19:0` on a raised stone support, glass `20:0` on
the west pad, and ice `79:0` on the east pad. The frozen signal includes
all three cells (`19+20+79`). Ice remains `79:0`; no torch is present, so
this compound is distinct from M386 melt. Those exact cells remain after
a clean save plus fresh login.

This is distinct from shipping M206 (sponge only), M196 (glass only),
M193 (ice place only), M308 (fragile ice-and-glass break plus torch
melt), and M386 (ice `79` to water `9` plus snow `78` to air). It does
not claim water absorption, glass pane, silk-touch, slipperiness, or
light-melt. Headless `B173WireClient` protocol-14 only. No GUI. No Aero.

The frozen semantic SHA-256 is
`0716150d188414cd60d0bebe7aa70f27ace8a376a47f6e0a912fc026e8ab63b5`.
