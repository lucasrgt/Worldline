# M287 purple wool

M287 opens the official purple-wool placement boundary. Packet15 of wool
item `35` with damage `10` on a raised stone support places block `35:10`.
That exact cell remains after a clean save plus fresh login.

This milestone is distinct from blue wool `35:11` and magenta wool
`35:2`. It does not claim other dye colors, sheep, or fire consumption.
Headless `B173WireClient` only. No GUI. No Aero.
