# M204 qualification cycle

`ClayCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places clay item `82` on the top face and reloads
block `82:0`.

The frozen semantic SHA-256 is
`3b76da4b3617c14a154aaf9799e7cd86631ba7e4e9be1d8dd1baf628fb271d8c`.

Canonical evidence uses two official server JVMs and four client sessions.
