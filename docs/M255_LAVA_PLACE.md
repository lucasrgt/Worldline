# M255 lava place

M255 opens the official lava-bucket place boundary. It is the inverse of
M181. The actor holds lava bucket `327` and uses Packet15 on a raised
four-wall stone basin cell, including direction-255 raytrace, so the
official `ItemBucket` path can write still lava.

The actor stands on the south wall so the basin cell is never occupied.
The live oracle is still lava `11:0` in the basin cell and Packet103
hotbar `325:1:0`. The same still lava and empty bucket persist after save
plus fresh login.

This milestone does not claim flowing-lava placement, obsidian reaction,
or fire spread.
