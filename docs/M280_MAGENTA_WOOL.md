# M280 magenta wool

M280 opens the official magenta-wool placement boundary. Packet15 of wool
item `35` with damage `2` on a raised stone support places block `35:2`.
That exact cell remains after a clean save plus fresh login.

This milestone is distinct from M197 white wool `35:0` and M248 orange
wool `35:1`. It does not claim other dye colors, sheep, or fire
consumption.
