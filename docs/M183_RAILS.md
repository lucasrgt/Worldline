# M183 rails

M183 opens the official rail-placement boundary. Packet15 of rail item
`66` on a raised stone support places block `66:0`. Actor look yaw `0`
pins that north-south facing so the result is deterministic. That exact
cell remains after a clean save plus fresh login.

This milestone does not claim powered rail, detector rail, minecarts,
curves, slopes, or redstone.
