# M264 qualification cycle

`RawPorkEatCycle` rebuilds the raw porkchop `319` fixture in two fresh official
server JVMs. Each run air-uses one raw porkchop, heals Packet8 `17 -> 20`,
consumes the stack, and reloads that inventory plus health.

The frozen semantic SHA-256 is
`c43583070f3c4185f97ebefa6a8f0a6ce3428f70ea6621d094546db1cbae4502`.

Canonical evidence uses two official server JVMs and four client sessions.
