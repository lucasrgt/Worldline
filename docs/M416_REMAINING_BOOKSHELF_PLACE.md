# M416 remaining bookshelf place

M416 opens the official remaining-bookshelf family. Packet102 on workbench
`58` crafts block `47` from six planks `5` plus three books `340`. Packet15
places at least two `47` cells in one session. Gold axe `286` Packet14
harvests one of those cells to air. Official Beta 1.7.3 bookshelves drop
nothing, so Packet21 book `340` is absent. The unharvested bookshelf
survives a clean save plus fresh login.

This is distinct from shipping M189 (one Packet15 of a single seeded
bookshelf) and from shipping M329 (workbench crafts of fence, ladder, and
one unplaced bookshelf). Book `340` harvest drops begin in Beta 1.9, not
in 1.7.3. Headless `B173WireClient` only. No GUI. No Aero.

The frozen semantic SHA-256 is
`63f78903270a88d3a7b5dafcb9aae55b9ffdacf7d785ff4b0f3d7616a975cc64`.
