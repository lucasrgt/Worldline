# M215 qualification cycle

`LapisBlockCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places lapis lazuli block item `22` on the top face
and reloads block `22:0`.

The frozen semantic SHA-256 is
`2eccf7bc9e04af5137d3804025114bea5686598ee5995596d8392a2a88b7fdbf`.

Canonical evidence uses two official server JVMs and four client sessions.
