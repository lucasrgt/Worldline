# M272 qualification cycle

`DiamondLeggingsCycle` equips diamond leggings `312` in two fresh
official server JVMs. Each run left-clicks window slot 7, proves peer
Packet5 slot 2, and reloads the equipped stack. One official EOF is
retried after a 5 second sleep.

The frozen semantic SHA-256 is
`84c5aee2ec930400dffd8c3fb58277fed715027fa6a538ea53d5acc3cd24b9a0`.

Canonical evidence uses two official server JVMs and eight client
sessions.

Run directly with:

```text
java tools/smoke/DiamondLeggingsCycle.java m272-diamond-leggings
```
