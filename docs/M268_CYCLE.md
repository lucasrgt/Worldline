# M268 qualification cycle

`FlintSteelFireCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run uses flint-and-steel item `259` on the top face, proves
live fire `51:0`, then requires the stone support and a valid fire-or-air cell
after a fresh login. One official EOF is retried after a 5 second sleep.

The frozen semantic SHA-256 is
`d9303a82df9a2cf43e607f47ba40e1b65d30f92b3e52819d1135a980a6eafafb`.

Canonical evidence uses two official server JVMs and four client sessions.
