# M191 qualification cycle

`GlowstoneCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places glowstone item `89` on the top face and
reloads block `89:0`.

The frozen semantic SHA-256 is
`3b17d9813ae06da188a84e5b3ea33feedbc0bc0f9c537be65e90e2dbf47f2187`.

Canonical evidence uses two official server JVMs and four client sessions.
