# M227 qualification cycle

`GoldOreCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places gold ore item `14` on the top face and
reloads block `14:0`.

The frozen semantic SHA-256 is
`0cc34fc524f7aba7d51b5f354569bbbfa7bae8bde9995797972fac9dea8ba1fd`.

Canonical evidence uses two official server JVMs and four client sessions.
