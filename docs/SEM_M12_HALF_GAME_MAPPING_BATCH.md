# SEM-M12 Half-Game Mapping Batch

SEM-M12 extends the same ordered, source-corroborated qualification plan to
3,238 identities, or 50 percent rounded up. It cannot substitute a new source,
silently change the graph, or reorder the smoke-first priority because its
policy pins the complete report digest.

The batch reuses the same four independently pinned mapping projects and the
same SEM-M6 official-bytecode coverage identity as SEM-M11. The exact policy is
`mappings/b1.7.3/sem-m12.properties`.
