# M196 qualification cycle

`GlassCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places glass item `20` on the top face and reloads
block `20:0`.

The frozen semantic SHA-256 is
`559e9d38c638c27b461ef834cc558063696964fbdff90e68a729877cb3daa13e`.

Canonical evidence uses two official server JVMs and four client sessions.
