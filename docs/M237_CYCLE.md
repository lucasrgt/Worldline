# M237 qualification cycle

`StoneCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places stone item `1` on the top face and
reloads block `1:0`.

The frozen semantic SHA-256 is
`a8969e296f04e9e9e445c08139a0fd689dc08bc6796515a90ad78d5b8e4f3ee9`.

Canonical evidence uses two official server JVMs and four client sessions.
