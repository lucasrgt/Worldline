# M228 qualification cycle

`DiamondOreCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places diamond ore item `56` on the top face and
reloads block `56:0`.

The frozen semantic SHA-256 is
`d8899da5d17c18d27351804645eead6a4a792f1d0497886d4bd24a15bedef72b`.

Canonical evidence uses two official server JVMs and four client sessions.
