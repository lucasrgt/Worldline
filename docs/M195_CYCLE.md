# M195 qualification cycle

`CobwebCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places cobweb item `30` on the top face and reloads
block `30:0`.

The frozen semantic SHA-256 is
`4ebaa9934454eff6fdfeec745c7a31c093ebc734fd77ade5eac818c4c3c8531a`.

Canonical evidence uses two official server JVMs and four client sessions.
