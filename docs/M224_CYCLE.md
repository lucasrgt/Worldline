# M224 qualification cycle

`NetherrackCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places netherrack item `87` on the top face and
reloads block `87:0`.

The frozen semantic SHA-256 is
`790c2fc12c97371a7b0a14f5a41376c1d23f3bd1fff998120baed91087fd917b`.

Canonical evidence uses two official server JVMs and four client sessions.
