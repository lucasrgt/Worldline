# M211 qualification cycle

`DoubleSlabCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places double slab item `43` on the top face and
reloads double-slab stone metadata `0`.

The frozen semantic SHA-256 is
`a5ad4aa55f65cbcb979ce018f5143d7d2036b7db34c77ed5f1369e2624fbf546`.

Canonical evidence uses two official server JVMs and four client sessions.
