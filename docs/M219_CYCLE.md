# M219 qualification cycle

`TntPlaceCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places TNT item `46` on the top face and reloads
unprimed TNT `46:0`.

The frozen semantic SHA-256 is
`4f2b9d0bc3bbd9a35010f5efbb02cf5d5e290dcfb590633b35833a02551912f8`.

Canonical evidence uses two official server JVMs and four client sessions.
