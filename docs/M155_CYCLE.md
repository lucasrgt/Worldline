# M155 qualification cycle

`MinecartSpawnCycle` rebuilds the raised rail fixture in two fresh official
server JVMs. Each run places rail `66` and minecart item `328`, then correlates
Packet23 type `10` across two peers.

The frozen semantic SHA-256 is
`8bbf2ce26b50b36cdb15763b126864882c8e138b89113c1fe6dcd75988703fab`.

Canonical evidence uses two official server JVMs and four client sessions.
