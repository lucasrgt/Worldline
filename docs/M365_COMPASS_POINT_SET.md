# M365 compass point set

M365 composes an official server boundary with an official client-rendering
boundary. `CompassPointSetCycle` proves compass item `345` is held, the server's
`level.dat` exposes `SpawnX/Y/Z`, the actor occupies two distinct cells, and the
stack plus fixture survive a save and fresh login.

Needle direction is not inferred from a copied trigonometric formula. The
pinned client differential drives the mapped and official `TextureCompassFX`
roots at east/west spawn positions with yaw `0` and `180`. Two fresh mapped
and two fresh official processes agree on smoothing state and complete pixel
digests. Nether random spin, compass crafting, clock behavior, and map use are
not claimed.

The frozen server signature is
`45694ff4da19a7b3c554d405380e9535c075d7fee1f3880674813ed5cc9a8b68`;
the frozen client signature is
`c2508b3dfff5f7852ce6b3155c5257ba781482031001cfdd38326b3363a5c014`.
Headless protocol-14 only. No Aero.
