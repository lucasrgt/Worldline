# M365 compass point set

M365 composes an official server boundary with an official client-rendering
boundary. `CompassPointSetCycle` proves compass item `345` is held, the server's
`level.dat` exposes `SpawnX/Y/Z`, the actor occupies two distinct cells, and the
stack plus fixture survive a save and fresh login.

Needle direction is not inferred from a copied trigonometric formula. A
separate pinned client differential must drive the mapped and official
`TextureCompassFX` roots at four controlled position/yaw arms before M365 can
qualify. Nether random spin, compass crafting, clock behavior, and map use are
not claimed.

The replacement server and client signatures remain pending the exclusive
official-runtime cycles. Headless protocol-14 only. No Aero.
