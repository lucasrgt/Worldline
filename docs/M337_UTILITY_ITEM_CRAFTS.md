# M337 utility item crafts

M337 opens the official compound utility-item craft boundary for shears
`359`, flint-and-steel `259`, and empty bucket `325` in one isolated cycle.

The actor first completes the two vanilla 2x2 recipes in personal window 0:
two iron ingots on the diagonal become shears, and one iron ingot plus flint
`318` become flint-and-steel. It then opens a placed workbench `58` and
crafts an empty bucket from three iron ingots. Packet102 take of each result
slot is accepted only when the official server returns those exact IDs. The
three stacks survive a clean save plus fresh login.

This milestone clones M297 personal 2x2 and M318 workbench crafting. It does
not claim M268 fire placement, M316 shears harvest, or filled-bucket use.

Frozen semantic SHA-256:
`24941b7cbf8eca87a6e5f03001a622de0dfb51a8d4e4f754906557bfa7603367`.
