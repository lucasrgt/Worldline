# M221 qualification cycle

`FurnacePlaceCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places furnace item `61` on the top face and reloads
idle furnace `61:2`.

The frozen semantic SHA-256 is
`88ee8a957cbe3c4c1f6f27f8b4fa73786709b04be0e37ea4c9b50e01737b965c`.

Canonical evidence uses two official server JVMs and four client sessions.
