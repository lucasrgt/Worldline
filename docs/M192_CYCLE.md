# M192 qualification cycle

`SoulSandCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places soul sand item `88` on the top face and
reloads block `88:0`.

The frozen semantic SHA-256 is
`4cf9190cca5bf84eabf13581b40e3e944a7c81c6c62f80bea686b7ff436ea63e`.

Canonical evidence uses two official server JVMs and four client sessions.
