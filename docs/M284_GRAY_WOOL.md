# M284 gray wool

M284 opens the official gray-wool placement boundary. Packet15 of wool
item `35` with damage `7` on a raised stone support places block `35:7`.
That exact cell remains after a clean save plus fresh login.

This milestone is distinct from light-gray wool `35:8` and black wool
`35:15`. It does not claim other dye colors, sheep, or fire consumption.
Headless `B173WireClient` only. No GUI. No Aero.
