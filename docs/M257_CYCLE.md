# M257 qualification cycle

`FurnaceMinecartCycle` rebuilds the raised rail fixture in two fresh official
server JVMs. Each run places rail `66:0` and furnace-minecart item `343`, then
correlates Packet23 type `12` across two peers.

The frozen semantic SHA-256 is
`57acb0174de88e73ae6725e8a676aa2dffb0d4b73fe19cbe462be5d882a70264`.

A transient official EOF sleeps five seconds and retries one fresh workspace.
Canonical evidence uses two official server JVMs and four client sessions.
