# M244 qualification cycle

`CakePlaceCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places cake item `354` on the top face and
reloads uneaten cake `92:0`.

The frozen semantic SHA-256 is
`3c7fa802b04a3eee353f1129e19f03bc24418ba2dfdab4b7598ebb480edc23fd`.

Canonical evidence uses two official server JVMs and four client sessions.
