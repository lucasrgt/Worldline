# M296 furnace smelts

M296 freezes three official furnace recipes together. Three idle furnaces
`61:2` load iron ore `15`, gold ore `14`, and raw pork `319` with coal
`263`. Packet103 publishes iron ingot `265`, gold ingot `266`, and cooked
pork `320` in output slot 2. Cook/burn telemetry is `199` / `1600` /
`1401`.

This milestone is a recipe family, not M60 sand-to-glass. It does not
claim output pickup or XP. Headless `B173WireClient` only. No GUI. No Aero.

The frozen semantic SHA-256 is
`04ac7e3f754356f848410854d026b17493e75b73dc55473905ec0d45d31787c1`.
