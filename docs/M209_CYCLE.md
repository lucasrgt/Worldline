# M209 qualification cycle

`LeavesCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places oak log item `17` adjacent first, then
leaves item `18` on the top face, and reloads leaf `18:8`.

The frozen semantic SHA-256 is
`aad065fc1b2eee7b0366a5095df49496d2002a0991c39ffefad789bb6896d5bd`.

Canonical evidence uses two official server JVMs and four client sessions.
