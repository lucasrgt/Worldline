# M267 qualification cycle

`MilkBucketCycle` rebuilds the raised stone basin plus milk-bucket fixture
in two fresh official server JVMs. Each run air-uses item `335`, freezes
held empty bucket `325` with unchanged health `20`, and reloads that
inventory plus health. One official EOF is retried after a 5 second sleep.

The frozen semantic SHA-256 is
`08a4bde6b39728f2585676409fd066a93aea351c0b561981b8e29c9fc7a2cff8`.

Canonical evidence uses two official server JVMs and four client sessions.
