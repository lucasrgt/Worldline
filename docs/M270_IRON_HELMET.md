# M270 iron helmet

M270 opens the official iron-helmet equipment boundary. It clones the M65
personal window-0 take/place pair, but the item is iron helmet `306` rather
than leather helmet `298`.

The actor left-clicks personal storage slot `36` then empty armor slot `5`.
Each predicted Packet102 is committed only after the correlated Packet106
acceptance. A named peer observes Packet5 slot `4` as item `306`. That same
window slot and peer equipment value survive a clean save plus fresh login.

Frozen semantic SHA-256:
`d62f78b5a3cb690f1845fa802de6bfa0cca27bc60ed090c60f93fdc665bf4f07`.

This milestone does not claim other iron pieces, gold, diamond, chain,
leather `298`, damaged armor, unequip, durability, or combat.
