# M213 qualification cycle

`IronBlockCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places iron block item `42` on the top face and
reloads block `42:0`.

The frozen semantic SHA-256 is
`f08a9fd9455cea30e230862721b12da696334532f35e67d3bcc977f3154ca81d`.

Canonical evidence uses two official server JVMs and four client sessions.
