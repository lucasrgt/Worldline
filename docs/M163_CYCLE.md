# M163 qualification cycle

`TrapdoorCycle` rebuilds the raised stone plus trapdoor fixture in two fresh
official server JVMs. Each run places item 96 against the east face, toggles
open then closed, and reloads the closed cell.

The frozen semantic SHA-256 is
`a93d386ba46f2b1dd44b2e91c9fc6c758d267a1b17cda83414f994a1d7d9d1a8`.

Canonical evidence uses two official server JVMs and four client sessions.
