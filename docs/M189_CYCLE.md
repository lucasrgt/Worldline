# M189 qualification cycle

`BookshelfCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places bookshelf item `47` on the top face and reloads
block metadata `0`.

The frozen semantic SHA-256 is
`30ee0483e42551b855a4fd5a85002dc5871168bc8f2bed26ec7dcd572b2b97a3`.

Canonical evidence uses two official server JVMs and four client sessions.
