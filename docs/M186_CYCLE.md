# M186 qualification cycle

`OakStairsCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places wooden stairs item `53` with look yaw `-90`
and reloads facing metadata `0`.

The frozen semantic SHA-256 is
`2af3b75842248ca27774c84635352ca60069d3ae89a045bfbe93d37a926c2ccd`.

Canonical evidence uses two official server JVMs and four client sessions.
