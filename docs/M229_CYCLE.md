# M229 qualification cycle

`RedstoneOreCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places redstone ore item `73` on the top face and
reloads unlit metadata `0`.

The frozen semantic SHA-256 is
`7d3985f4b7402dfe18498e350a010e0aa42df1a87bbb12ac06d7ff9bc4803504`.

Canonical evidence uses two official server JVMs and four client sessions.
