# M178 qualification cycle

`JukeboxCycle` rebuilds the raised stone plus jukebox fixture in two fresh
official server JVMs. Each run places item `84`, inserts gold disc `2256`,
and reloads block `84:1`.

The frozen semantic SHA-256 is
`97de107318c1552893e50f28f3483ac127f615e7ae8b5018e70e651c21886a86`.

Canonical evidence uses two official server JVMs and four client sessions.
