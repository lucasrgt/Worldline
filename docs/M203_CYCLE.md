# M203 qualification cycle

`SnowLayerCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places snow layer item `78` on the top face and
reloads live block `78:0`.

The frozen semantic SHA-256 is
`23163bc78dd8ce876aff379a6292ad170fe0b74e5c26f2ee7177508222aa0178`.

Canonical evidence uses two official server JVMs and four client sessions.
