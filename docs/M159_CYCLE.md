# M159 qualification cycle

`SugarCaneCycle` rebuilds the raised dirt-and-still-water fixture in two fresh
official server JVMs. Each run plants cane `83` with item `338` and waits a
bounded random-tick window until height `>= 2`.

The frozen semantic SHA-256 is
`70a49f193de25db52e447675752317f7e567b9817943e16a27eb25a669555d8f`.

Canonical evidence uses two official server JVMs and four client sessions.
