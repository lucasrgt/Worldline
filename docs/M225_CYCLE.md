# M225 qualification cycle

`CoalOreCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places coal ore item `16` on the top face and
reloads block `16:0`.

The frozen semantic SHA-256 is
`2ff6ed07ba198e90c82b36fb04ced28cc6e6900e1ccca71d4ee74554803e53fd`.

Canonical evidence uses two official server JVMs and four client sessions.
