# M185 qualification cycle

`DetectorRailCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places detector rail item `28` on the top face and
reloads unpowered facing metadata `0`.

The frozen semantic SHA-256 is
`81cc57ce0d8d5c637a58696af5d3d47097bd3d14016813703c4f7718cb9505a2`.

Canonical evidence uses two official server JVMs and four client sessions.
