# M173 qualification cycle

`FenceCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places two adjacent fence items `85` as `85:0`
and reloads both cells.

The frozen semantic SHA-256 is
`87da22ea0cb364e70239d8989a677f835f86ebbe9768dee6955f04ee4be1f74e`.

Canonical evidence uses two official server JVMs and four client sessions.
