# M283 qualification cycle

`PinkWoolCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places pink wool item `35` (damage `6`) on the
top face, freezes live `35:6`, and reloads that cell after save plus a
fresh login.

The frozen semantic SHA-256 is
`54b69bd3555ea1c71c7bfe4a627ef3aebad41301df717ac15f1338148b198863`.

Canonical evidence uses two official server JVMs and four client sessions.
