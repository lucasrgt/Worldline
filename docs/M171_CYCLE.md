# M171 qualification cycle

`PumpkinCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places pumpkin item `86` with look yaw `-90` and
reloads facing metadata `1`.

The frozen semantic SHA-256 is
`239e38d65add8b63f1afa9dba90e1832f5b70b91bf6b6e5fee0df381e48c12e0`.

Canonical evidence uses two official server JVMs and four client sessions.
