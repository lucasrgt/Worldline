# M259 qualification cycle

`CookedPorkEatCycle` rebuilds the raised stone plus cooked-pork fixture in two
fresh official server JVMs. Each run air-uses item `320`, heals Packet8
`12 -> 20`, consumes the stack, and reloads the empty hotbar slot.

The frozen semantic SHA-256 is
`c287f963780070b55c9773bcc0ad5b914a8c6a7713870dd9d5533eda3d449b0e`.

Canonical evidence uses two official server JVMs and four client sessions.
