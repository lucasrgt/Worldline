# M216 qualification cycle

`ObsidianCycle` rebuilds the M175 raised stone fixture in two fresh
official server JVMs. Each run places obsidian item `49` on the top face
and reloads block `49:0`.

The frozen semantic SHA-256 is
`37b0c135b01ef8c65cbbb0636a135f26610fb0825a3acf378e396d5150a32bce`.

Canonical evidence uses two official server JVMs and four client sessions.
