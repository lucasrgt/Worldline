# M168 qualification cycle

`WaterBucketCycle` rebuilds the raised stone basin in two fresh official
server JVMs. Each run places still water `9:0`, picks it up with empty
bucket `325`, and reloads the empty cell plus water bucket `326:1:0`.

The frozen semantic SHA-256 is
`4ce39f3401e15de5c720a314091f69acf985c459785d211d52f84f4af9e47a7d`.

Canonical evidence uses two official server JVMs and four client sessions.
