# M175 torch

M175 opens the official floor-torch placement boundary. Packet15 of torch
item `50` on a raised stone support places block `50:5`. That exact
floor-torch metadata remains after a clean save plus fresh login.

This milestone does not claim wall-torch facings as extra hashed variants,
redstone torch, or the lighting plane.
