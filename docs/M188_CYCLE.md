# M188 qualification cycle

`StoneSlabCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places slab item `44` on the top face and reloads
single-slab stone metadata `0`.

The frozen semantic SHA-256 is
`58238d33d76d1cd336bbd528ecfe662b6e821d1118c643e6c3023f1b18c800f1`.

Canonical evidence uses two official server JVMs and four client sessions.
