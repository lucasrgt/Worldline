# M261 qualification cycle

`GoldenAppleEatCycle` seeds golden apple `322` plus Health `10` in two fresh
official server JVMs. Each run air-uses the stack, freezes Packet8
`10 -> 20`, and reloads the consumed inventory.

The frozen semantic SHA-256 is
`888e7fe8215ede44deaf9a73fa95ecc769f61e554e9262d0781ae75eca2e9fe3`.

Canonical evidence uses two official server JVMs and four client sessions.
