# M235 qualification cycle

`WoodSlabCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places slab item `44` damage `2` on the top face
and reloads single-slab wood metadata `2`.

The frozen semantic SHA-256 is
`db22e6770987d78c1a09404991df3d73037770d68f7fb9215a20f0e9a4f383fa`.

Canonical evidence uses two official server JVMs and four client sessions.
