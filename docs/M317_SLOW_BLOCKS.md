# M317 slow blocks

M317 composes two independent official boundaries. `SlowBlocksCycle` proves
server-authored placement and fresh-login persistence of cobweb `30:0` and soul
sand `88:0` in two fresh dedicated-server JVMs. The controlled client cycle
drives the mapped `EntityPlayerSP.moveEntityWithHeading` root through
`B173PhysicsProbe.slowBlocks` and compares its air, cobweb, and soul-sand trace
with the exact official obfuscated root in two additional JVMs.

No Packet13 displacement is authored as a physics oracle. The server scenario
does not calculate slowdown factors, and the client probe does not substitute a
Worldline movement equation. Qualification requires both frozen traces and the
mapped/official client trace to match exactly.

The replacement server signature and shared client-physics signature remain
pending until the exclusive official-runtime cycle completes. No GUI. No Aero.
