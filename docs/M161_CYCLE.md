# M161 qualification cycle

`SnowballCycle` first executes the production Packet23 byte fixture, then
rebuilds the raised snowball throw in two fresh official server JVMs. Each run
correlates one identical type-`61` object packet across two clients and
enforces identity, thrower and bounded-pose invariants.

The frozen semantic SHA-256 is
`1865bda95354d11a5a95ac4eaaf3fb8ad521e4b6195e6c651715e6955e7a149d`.

Canonical evidence uses two official server JVMs and four client sessions.
