# M160 qualification cycle

`CakeEatCycle` rebuilds the raised stone plus `BlockCake` fixture in two fresh
official server JVMs. Each run places cake item 354, eats one slice, and
reloads the bitten block.

The frozen semantic SHA-256 is
`ec442ce01a43be294030f3c3fb48319afc75e6eb99291a4e24cab8a54f3d8074`.

Canonical evidence uses two official server JVMs and four client sessions.
