# M429 remaining attach faces

M429 opens the official remaining wall-attach facing set. Packet15 of
ladder item `65`, trapdoor item `96`, and sign item `323` against the
west, south, and north faces of one three-cell raised stone column places
`65:4`/`65:3`/`65:2`, closed `96:2`/`96:1`/`96:0`, and wall signs
`68:4`/`68:3`/`68:2` in one session. The frozen signal names those nine
damages. All nine cells survive a clean save plus fresh login.

This is distinct from shipping M361 (east ladder `65:5` Packet13 climb),
from shipping M380 (trapdoor `96` four-face open then close), and from
shipping M350 (standing `63` plus wall `68` Packet130 text). It does not
claim east attachments `65:5`/`96:3`/`68:5`, climb physics, trapdoor
toggle, standing sign `63`, or sign text. Headless `B173WireClient` only.
No GUI. No Aero.

The frozen semantic SHA-256 is
`d00079b30c3f58f9f2a197e5a0a27c88880e15c28c3eaf88806d4502ebc2eb2b`.
