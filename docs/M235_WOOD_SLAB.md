# M235 wood slab

M235 opens the official wood-slab placement boundary. Packet15 of slab
item `44` with damage `2` on a raised stone support places block `44:2`.
That exact single-slab wood metadata remains after a clean save plus
fresh login.

This milestone does not re-qualify stone slab `44:0`. It does not claim
sandstone or cobblestone slab variants.
