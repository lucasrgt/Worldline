# M202 qualification cycle

`SaplingCycle` rebuilds the raised dirt-capped stone fixture in two
fresh official server JVMs. Each run places oak sapling item `6` on
dirt `3` and reloads block `6:0`.

The frozen semantic SHA-256 is
`7772115ec090ef211b01204fa558371ea9983994367b0ceb0899a44441bdb24d`.

Canonical evidence uses two official server JVMs and four client sessions.
