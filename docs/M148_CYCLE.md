# M148 qualification cycle

`PigAiMovementCycle` builds the complete grass-platform/spawner fixture in two
fresh official server JVMs. Each run uses two peers and must reproduce the same
categorical spawn/movement contract without freezing the random direction.

The frozen semantic SHA-256 is
`c265a9aa7d1e6254b11458774346f05613c82569948443803f4742740e933397`.

Canonical evidence uses two official server JVMs and four client sessions.
