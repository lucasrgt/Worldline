# M425 remaining machine faces

M425 opens the official remaining look-yaw facing family for dispenser
`23`, furnace `61`, and pumpkin `86` together. Packet15 of dispenser item
`23` places `23:2`, `23:5`, and `23:4` from look yaw `0`, `90`, and `-90`.
Packet15 of furnace item `61` places `61:5`, `61:3`, and `61:4` from look
yaw `90`, `180`, and `-90`. Packet15 of pumpkin item `86` places `86:2`,
`86:3`, and `86:0` from look yaw `0`, `90`, and `180`. The frozen signal
names all three blocks plus multiple remaining facing metas. All nine
cells survive a clean save plus fresh login.

This is distinct from shipping M231, M221, and M171 (one place-only facing
each). It does not claim dispenser eject, Trap windows, furnace smelt,
burning furnace `62`, jack-o-lantern `91`, or snow/iron golems. Headless
`B173WireClient` only. No GUI. No Aero.

The frozen semantic SHA-256 is
`5f5f8026b3aef5768a963db53d9393ac9ed86b766118d805407d5cf5b11a5dbf`.
