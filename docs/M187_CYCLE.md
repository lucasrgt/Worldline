# M187 qualification cycle

`CobbleStairsCycle` rebuilds the raised stone fixture in two fresh
official server JVMs. Each run places cobblestone stairs item `67` with
look yaw `-90` and reloads facing metadata `0`.

The frozen semantic SHA-256 is
`a569478836d30464768b7bc64d771b5cd735b7fb65e33ab5bb5a661e1f318a96`.

Canonical evidence uses two official server JVMs and four client sessions.
