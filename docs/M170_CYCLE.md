# M170 qualification cycle

`RepeaterCycle` rebuilds the raised west-facing repeater line in two fresh
official server JVMs. Each run places item 356 as `93:3`, pulses it through
an input-side lever to `94:3` and back to `93:3`, and reloads the unpowered
diode.

The frozen semantic SHA-256 is
`6c15d889fbdd2c03553d0456cd4206acca9913147855898da285810b5cffe59b`.

Canonical evidence uses two official server JVMs and six client sessions.
