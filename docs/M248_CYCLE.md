# M248 qualification cycle

`OrangeWoolCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places orange wool item `35` (damage `1`) on the
top face, freezes live `35:1`, and reloads that cell after save plus a
fresh login.

The frozen semantic SHA-256 is
`89d3e625a2e980af7b569af8fc82e46b0e5ecff6f79e8090cabff02a38496590`.

Canonical evidence uses two official server JVMs and four client sessions.
