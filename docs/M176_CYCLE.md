# M176 qualification cycle

`SignCycle` rebuilds the raised stone plus standing-sign fixture in two
fresh official server JVMs. Each run places item `323`, writes Packet130
ASCII lines, and reloads the same tile text.

The frozen semantic SHA-256 is
`02572936a90d996c81d67465e5507ff4f5ecb33262d10fd9cdb0e2cbe28489ff`.

Canonical evidence uses two official server JVMs and four client sessions.
