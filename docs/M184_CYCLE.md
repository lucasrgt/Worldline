# M184 qualification cycle

`PoweredRailCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places powered-rail item `27` on the top face and
reloads unpowered facing metadata `0`.

The frozen semantic SHA-256 is
`d017b9f94e15a87ad2465679091958308c683a77948158ee7b9b3fb241c56264`.

Canonical evidence uses two official server JVMs and four client sessions.
