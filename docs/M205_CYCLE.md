# M205 qualification cycle

`BrickCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places brick block item `45` on the top face and
reloads block `45:0`.

The frozen semantic SHA-256 is
`e086305d19013c746cd1e24ea91a5cf8c20cb87ac8b3e0d6c79d11b224a1ac90`.

Canonical evidence uses two official server JVMs and four client sessions.
