# M157 qualification cycle

`BowArrowCycle` rebuilds the raised stone platform in two fresh official
server JVMs. Each run fires one bow and requires both peers to decode the
same Packet23 type-60 arrow.

The frozen semantic SHA-256 is
`abf0244450acb0b727df9080a6ca53849fcd0ca4ce62de83a13b815d04c8f917`.

Canonical evidence uses two official server JVMs and four client sessions.
