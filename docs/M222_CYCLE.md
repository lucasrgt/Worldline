# M222 qualification cycle

`CobbleCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places cobblestone item `4` on the top face and
reloads block `4:0`.

The frozen semantic SHA-256 is
`b85fbf8097da42d8a630be610e6ab4457bd0de302e1f3396daff2d3fd161ac20`.

Canonical evidence uses two official server JVMs and four client sessions.
