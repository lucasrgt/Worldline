# M211 double slab

M211 opens the official double-slab placement boundary. Packet15 of double
slab item `43` on a raised stone support places block `43:0`. That exact
double-slab stone metadata remains after a clean save plus fresh login.

This milestone does not re-qualify single slab `44`. It does not claim
sandstone, wood, or cobblestone double variants unless the official server
writes them from this fixture.
