# M236 qualification cycle

`CobbleSlabCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places slab item `44` with damage `3` on the top
face and reloads cobble-slab metadata `3`.

The frozen semantic SHA-256 is
`0659df1d047139297efabd6988985a6f3c1bb9b16e6dfa93f6bc1387d8dbc335`.

Canonical evidence uses two official server JVMs and four client sessions.
