# M181 qualification cycle

`LavaBucketCycle` rebuilds the raised stone basin in two fresh official
server JVMs. Each run places still lava `11:0`, picks it up with empty
bucket `325` from the south wall, and reloads the empty cell plus lava
bucket `327:1:0`.

The frozen semantic SHA-256 is
`8389064523049de74163fc5f5c48e14d5e52eb750aee3eb297010fa2e87116d4`.

Canonical evidence uses two official server JVMs and four client sessions.
