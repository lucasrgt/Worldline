# M245 qualification cycle

`WallSignCycle` rebuilds the raised stone plus wall-sign fixture in two
fresh official server JVMs. Each run places item `323` against an east
face, writes Packet130 UCS-2 lines, and reloads the same tile text.

The frozen semantic SHA-256 is
`124ca56d12f9c02d6f8463c6ad28739dcc1c7b29875b4fa356570082c5f82c06`.

Canonical evidence uses two official server JVMs and four client sessions.
