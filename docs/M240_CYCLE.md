# M240 qualification cycle

`BedPlaceCycle` rebuilds the raised stone-and-bed fixture in two
fresh official server JVMs. Each run places bed item `355` from look
yaw `0` and reloads the unoccupied `26:0` / `26:8` halves.

The frozen semantic SHA-256 is
`366879f4dbd3ab1b199692d6094ad12c0ee76aa41469342f4ee3fba3d74ec59d`.

Canonical evidence uses two official server JVMs and four client sessions.
