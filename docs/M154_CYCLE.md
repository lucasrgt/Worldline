# M154 qualification cycle

`BoatSpawnCycle` rebuilds the natural water fixture in two fresh official
server JVMs. Each run places one boat and correlates Packet23 across two peers.

The frozen semantic SHA-256 is
`5da646e53a2e386476060c80fb7c8bce2d187f93133cb6adb76ac439e48439a6`.

Canonical evidence uses two official server JVMs and four client sessions.
