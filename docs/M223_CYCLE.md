# M223 qualification cycle

`DirtCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places dirt item `3` on the top face and reloads
block `3:0`.

The frozen semantic SHA-256 is
`cb51b4a07fd7c818ad09e7ea60fe06e3c01a3f3bca23a3c618cfe10d5a5cb650`.

Canonical evidence uses two official server JVMs and four client sessions.
