# M242 qualification cycle

`LeverPlaceCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places lever item `69` against the east face and
reloads unpowered facing metadata `1`.

The frozen semantic SHA-256 is
`28f6b893342410779d684e2473574c663af5c667ff857e946aa225e71f8f69bf`.

Canonical evidence uses two official server JVMs and four client sessions.
