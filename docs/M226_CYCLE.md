# M226 qualification cycle

`IronOreCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places iron ore item `15` on the top face and
reloads block `15:0`.

The frozen semantic SHA-256 is
`a0e1b3a34325710f72942c88c73bd15a8cad197fb7b04c8bbfc5f867b97389b7`.

Canonical evidence uses two official server JVMs and four client sessions.
