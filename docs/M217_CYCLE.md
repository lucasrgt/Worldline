# M217 qualification cycle

`MossyCobbleCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places mossy cobblestone item `48` on the top face and
reloads block `48:0`.

The frozen semantic SHA-256 is
`e7a7a8e7b99473c5e1e123d0d2542636867e0e5ebae855883295e487ee937a43`.

Canonical evidence uses two official server JVMs and four client sessions.
