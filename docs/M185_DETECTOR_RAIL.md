# M185 detector rail

M185 opens the official detector-rail placement boundary. Packet15 of
detector rail item `28` on a raised stone support places block `28:0`.
No minecart is present, so the detector bit stays 0. That exact unpowered
facing metadata remains after a clean save plus fresh login.

This milestone does not claim powered rails, regular rails, or minecart
activation of the detector bit.
