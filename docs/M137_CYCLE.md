# M137 qualification cycle

`TntExplosionCycle` repeats one isolated TNT ignition in two fresh official
server JVMs. Both runs prove the same constructed origin, strength `4`, a
positive Packet60 destroyed-cell list containing the stone support, live cache
updates to air and fresh-session persistence.

The exact destroyed count stays outside the signature because vanilla gives
the primed entity randomized motion before detonation. The frozen semantic
SHA-256 is
`bb96106b407266a1f02f9e9e8097e71f5d11de9337293e8cf063277cd00f07ed`.

Canonical evidence uses two official server JVMs and four client sessions.
