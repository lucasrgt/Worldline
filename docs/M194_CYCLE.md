# M194 qualification cycle

`SnowBlockCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places snow block item `80` on the top face and
reloads live block `80:0`.

The frozen semantic SHA-256 is
`27b0f59762b6d741c75eb15488c5800f88feb7ca971582557e47532e6cc98a83`.

Canonical evidence uses two official server JVMs and four client sessions.
