# M150 qualification cycle

`PigPorkDropCycle` rebuilds the grass-platform/spawner fixture in two fresh
official server JVMs. Each run uses two peers, reuses the adapter-owned pig
identity through movement and death, and requires one shared Packet21 porkchop.

The frozen semantic SHA-256 is
`90cf54607ffd52b403765121c14d821e80e9996702f158c29efe63aee15b0d33`.

Canonical evidence uses two official server JVMs and four client sessions.
