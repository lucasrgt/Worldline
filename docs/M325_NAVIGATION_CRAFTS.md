# M325 navigation crafts

M325 opens the official workbench-craft boundary for compass `345`, clock
`347`, and empty map `358` in one isolated compound cycle.

The actor places workbench `58` on the raised-stone fixture, opens that
window, and completes the vanilla recipes: iron plus redstone to compass,
gold plus redstone to clock, and paper plus compass to empty map. Packet102
take of each result slot is accepted only when the official server returns
those exact IDs. Compass, clock, and map survive a clean save plus fresh
login.

Frozen semantic SHA-256:
`904591e822865303c647ea818403edb8d115b37da19262cb96387da6f2e4302d`.

This milestone does not claim map use, filled-map damage, compass/clock
GUI, or any other workbench recipe.
