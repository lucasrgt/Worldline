# M409 spider string set

M409 opens the official spider-web family. One saved mob spawner is
retargeted from `Pig` to `Spider`. After midnight (`time set 14000`) the
headless protocol-14 client observes Packet24 type `52`, kills it with
Packet7 diamond sword `276`, and collects Packet21 string `287`. The same
session Packet15-places cobweb `30:0`. This is distinct from M388's zombie
feather `288` plus skeleton arrow `262`.

Frozen semantic SHA-256:
`625db8d839633b99daf0e73b098ad644d6f23e3d9ed4dda4f187687c1fe26fc4`.

This milestone does not claim cobweb persistence (M195), cobweb-to-string
harvest, XP, breeding, despawn, loot-table counts, or other hostile types.
Headless `B173WireClient` only. No GUI. No Aero.
