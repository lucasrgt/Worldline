# M253 qualification cycle

`GreenWoolCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places wool item `35` (damage `13`) on the
top face, freezes live `35:13`, and reloads that cell after save plus a
fresh login.

The frozen semantic SHA-256 is
`dafe191ebebe5c80fb8935d352a73bebfb997614628047215ffde468c90be210`.

Canonical evidence uses two official server JVMs and four client sessions.
