# M457 spider leap set

M457 opens the official spider leap-plus-contact family. One saved mob
spawner is retargeted from `Pig` to `Spider`. After midnight (`time set
14000`) the headless protocol-14 client observes Packet24 type `52` leap
toward the actor on the flat grass pad, then touch Packet8/38 hurt.
Golden apple `322` restores health. This family is leap plus contact
damage, not climb.

This milestone does not claim spider climb (M447), string `287` plus
cobweb `30` (M409), or night-time natural hostile identity among
`50/51/52/54` (M435). Headless `B173WireClient` only. No GUI. No Aero.

The frozen semantic SHA-256 is
`c1acc30fb89383a980963eda9ae54bd6fcc4a2c8eaff785ee3a10b3206e3153c`.
