# M358 snow craft set

M358 opens the official compound snow craft-and-harvest boundary. Four
snowballs `332` in the personal 2x2 grid craft snow block `80`. Packet14
while holding gold shovel `284` then breaks player-placed snow layer `78`
and snow block `80` to Packet21 snowball `332`. Both result ids `80` and
`332` appear in the frozen signal.

Frozen semantic SHA-256:
`d35de53474c363b1b580a865ea4bcce9403b8f9092e3ca5be19e9f1bf6e6d1be`.

This milestone does not claim M194 snow-block placement, M203 snow-layer
placement, M161 snowball throws, or shovel durability. Headless
`B173WireClient` protocol-14 only. No GUI. No Aero.
