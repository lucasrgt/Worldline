# M258 qualification cycle

`BreadEatCycle` rebuilds the bread `297` fixture in two fresh official server
JVMs. Each run air-uses one bread, heals Packet8 `15 -> 20`, consumes the
stack, and reloads that inventory plus health.

The frozen semantic SHA-256 is
`1f0cbe46155bbaf393891dc8f4343effa6b5f502c8efc4c8f0122c424a05da3a`.

Canonical evidence uses two official server JVMs and four client sessions.
