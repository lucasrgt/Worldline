# M199 rose

M199 opens the official red-rose placement boundary. Packet15 of rose item
`38` on dirt or grass places block `38:0`. Stone rejects that placement, so
the fixture places dirt `3` first and then rose `38`. That exact cell remains
after a clean save plus fresh login.

This milestone does not claim dandelion `37`, bone meal, or dye.
