# M364 qualification cycle

`LightOpacitySetCycle` rebuilds the raised stone fixture in two fresh
official server JVMs. Each run Packet15-places glass `20`, log-backed
leaves `18:8`, and ice `79`, then reloads Packet51 sky-light and
block-light nibbles at those cells. The frozen signal includes those
block ids plus light values. One official EOF is retried after a 5
second sleep.

Run directly with:

```text
java tools/smoke/LightOpacitySetCycle.java m364-light-opacity-set
```

Canonical evidence uses two official server JVMs and four client sessions.
Headless protocol-14 only. No GUI. No Aero.

The frozen semantic SHA-256 is
`2012aa0391268a287bc772ea5a40036b761ed9f72b90d91f8462512dbb0e3fab`.
