# M205 brick

M205 opens the official brick-block placement boundary. Packet15 of brick
block item `45` on a raised stone support places block `45:0`. That exact
cell remains after a clean save plus fresh login.

This milestone does not claim clay smelting or brick item `336`.
