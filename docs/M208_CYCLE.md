# M208 qualification cycle

`OakLogCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places wood/log item `17` (damage `0`) on the top
face, freezes live `17:0`, and reloads that cell after save plus a
fresh login.

The frozen semantic SHA-256 is
`c371df4ca97d388218b9184b3b6a0ba2745803de01514b4b8562c6ca33b533d2`.

Canonical evidence uses two official server JVMs and four client sessions.
