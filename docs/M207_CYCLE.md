# M207 qualification cycle

`SandstoneCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places sandstone item `24` on the top face and
reloads block `24:0`.

The frozen semantic SHA-256 is
`f1168be76eb874a213a7c540fcc667aeb929883a30fe9ccb00676cf74cf65b8e`.

Canonical evidence uses two official server JVMs and four client sessions.
