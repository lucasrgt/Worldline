# M231 qualification cycle

`DispenserPlaceCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places dispenser item `23` with look yaw `180` and
reloads facing metadata `3`.

The frozen semantic SHA-256 is
`0679d7a20880a59f567737898c4d0263b285b13b738810981188b7b8b8fbecf4`.

Canonical evidence uses two official server JVMs and four client sessions.
