# M252 qualification cycle

`BlueWoolCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places blue wool item `35` (damage `11`) on the
top face, freezes live `35:11`, and reloads that cell after save plus a
fresh login.

The frozen semantic SHA-256 is
`9df82196251a63865e986ad531cff422ad86ee987524f8c25679c38143ac80a3`.

Canonical evidence uses two official server JVMs and four client sessions.
