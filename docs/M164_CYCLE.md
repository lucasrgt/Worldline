# M164 qualification cycle

`PressurePlateCycle` rebuilds the raised stone plate fixture in two fresh
official server JVMs. Each run places stone plate item `70`, powers the cell by
standing on it, depowers by stepping off, and reloads unpowered `70:0`.

The frozen semantic SHA-256 is
`ab14f3bebb0157e814af07dd4950065b472c5d5b99f25736c02b57fd08b1f754`.

Canonical evidence uses two official server JVMs and four client sessions.
