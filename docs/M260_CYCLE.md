# M260 qualification cycle

`AppleEatCycle` rebuilds the raised stone plus apple-260 fixture in two fresh
official server JVMs. Each run air-uses apple item 260, heals Packet8
`16 -> 20`, consumes the stack, and reloads that player state.

A transient official EOF sleeps five seconds and retries one fresh workspace.

The frozen semantic SHA-256 is
`f5122f857247406ea443e90df7cb0b2f8b8bfd0ef8f151b677d9b3f8a4598130`.

Canonical evidence uses two official server JVMs and four client sessions.
