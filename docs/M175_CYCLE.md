# M175 qualification cycle

`TorchCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places torch item `50` on the top face and reloads
floor-torch metadata `5`.

The frozen semantic SHA-256 is
`1b9a95028d397daf378283e42e4657f27df1e1e761003ef151f0fdd1790c3c3d`.

Canonical evidence uses two official server JVMs and four client sessions.
