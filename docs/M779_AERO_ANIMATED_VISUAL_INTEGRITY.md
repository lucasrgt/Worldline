# M779-AERO-ANIMATED-VISUAL-INTEGRITY Aero animated visual integrity

<!-- Generated from smoke.properties by MilestoneNarrative. -->

## Claim

Four fresh JVMs in ABBA order require Aero broad conservative cone culling, fixed immutably at process startup, to introduce no framebuffer difference outside the independently observed same-arm raster noise across deterministic keyframed animation, morph deformation, player-targeted IK, and production animated-to-at-rest LOD transitions while preserving repeatable same-arm work and reducing animated work and render time in both rounds.

## Qualification cycle

M779 restores the same frozen 120-model world into four fresh clients ordered cull-off, cull-on, cull-on, cull-off. Each client pre-traverses the complete route five times for 1,200 unmeasured frames under its immutable arm. The pinned Aero runtime orders equal-state animated batches by stable model resource name, uses each model's visual-radius sphere at the broad-cone boundary, collapses previous/current camera interpolation at every moving sample, seeks all keyframed, morph, and IK models to spatially varied deterministic poses on each of 240 measured route frames, crosses production animation LOD during orbit, walk, fast-spin, teleport, and near-panel movement, and captures 24 full RGBA frames. The differential oracle builds a noise mask from off/off and on/on repeats, limits each pair and the union mask to 10 changed locations per million observed pixels, forbids every off/on change outside that independent mask, requires animated, at-rest, and list-call work to repeat exactly within each arm, and records render duration and 50 ms hitches.

Expected signal: `scene=120-dynamic-40keyframed+40morph+40ik,jvms=4-fresh-abba,route=orbit+walk+fast-spin+teleport+near-orbit,lod=animated+at-rest-transition,frames=960,culling=immutable-startup-radius-aware-broad-cone,pixels=0-unexplained+raster-noise<=10ppm,work+render=reduced2of2,hitches=censused`.

Frozen semantic SHA-256: `dd3575df61a9f9ab10a054b9a40f4547375a9280368ce40799de5d2f55f97a84`.
