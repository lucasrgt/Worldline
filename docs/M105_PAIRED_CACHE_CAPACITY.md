# M105 paired cache capacity

Status: GO in Worldline v1.93.0.

M105 directly compares the pinned cell-page cache at capacity one and at the
literal negative-one unlimited sentinel. Both arms keep pages enabled, rebuild
sentinel negative one, TTL100000, the same sixteen synchronized identities,
exact camera, renderer path, and recorder. Each pair shares its plan and nonce;
all processes and worlds remain fresh.

Before teleport, the server places and verifies one vanilla stone support
block outside the sixteen-cell Aero fixture. The full strict pose gate remains
unchanged in both arms.

Capacity one is expected to thrash the stable four-page scene: all four pages
rebuild, only one remains cached, and eviction advances by four in every
retained record. Unlimited capacity is expected to warm four pages before the
window and then retain them with no rebuilds or evictions. Both arms must make
four page calls and must never use either immediate or direct fallback.

This qualifies the exact `< 0` unlimited-capacity branch in the pinned Aero
revision. It does not estimate memory usage or claim that unlimited caching is
generally preferable. Timing summaries remain descriptive observations only.

Nonclaims: generic cache policy, memory cost, uninstrumented/additive cost,
causality, regression/improvement, statistical inference, pixels,
cross-machine generality, combat, or historical lag attribution.
