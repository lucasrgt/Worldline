# M237 stone

M237 opens the official stone placement boundary. Packet15 of stone
item `1` on a raised stone support places block `1:0`. That exact
cell remains after a clean save plus fresh login.

This milestone does not claim cobblestone item `4` or stone slab.
