# M106 paired minimum instances

Status: GO in Worldline v1.94.0.

M106 compares literal `aero.becell.minInstances` values two and five under one
exact sixteen-identity scene. Both arms keep pages enabled, cache capacity and
rebuild budget at negative-one unlimited, TTL100000, and the same paired plan,
nonce, camera, renderer, and primitive recorder.

The automatic plan forms four natural page keys. At minimum two all four keys
are cached and replayed. At minimum five two page populations remain eligible,
while the two smaller populations route four instances through the direct
fallback. Every retained record proves `pageCalls/direct/cached` of `4/0/4`
versus `2/4/2`, with rebuild, eviction, and immediate counters all zero.

The aligned M74 census independently observes render/list `0/0` versus `4/4`.
The server still owns X/Y/Z and the client validates them strictly. The test
client fixes only yaw/pitch before readiness so physical mouse input cannot
change the paired camera.

This qualifies the exact mixed threshold behavior at literals two and five in
the pinned Aero revision. It does not claim a generally preferable threshold.
Timing summaries remain descriptive observations only.

Nonclaims: generic threshold policy, memory cost, uninstrumented/additive cost,
causality, regression/improvement, inference, pixels, cross-machine
generality, combat, or historical lag attribution.
