# M174 qualification cycle

`LadderCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places ladder item `65` against the east face and
reloads facing metadata `5`.

The frozen semantic SHA-256 is
`901e76a85d36008f4429e6863549902a4c2b49485980fbfb65a9568725bb491e`.

Canonical evidence uses two official server JVMs and four client sessions.
