# M234 qualification cycle

`SandstoneSlabCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places slab item `44` with damage `1` on the top face
and reloads sandstone-slab metadata `1`.

The frozen semantic SHA-256 is
`607498ca23a0859d9a2296eba8fb8d4c4c407a3af84801eb6ccf552314e65f2a`.

Canonical evidence uses two official server JVMs and four client sessions.
