# M269 qualification cycle

`ShearsLeavesCycle` rebuilds the raised stone fixture in two fresh
official server JVMs. Each run places oak log item `17` adjacent first,
then two leaves items `18`, shears one leaf with item `359`, and
bare-hand breaks the other. One official EOF is retried after a 5 second
sleep.

The frozen semantic SHA-256 is
`ea2a38e965fa7a2b9bf0d278cfd600efa328aba31fa9c3c991c85eb408a2953e`.

Canonical evidence uses two official server JVMs and two client sessions.
