# M214 qualification cycle

`DiamondBlockCycle` rebuilds the raised stone fixture in two fresh official
server JVMs. Each run places diamond block item `57` on the top face and
reloads block `57:0`.

The frozen semantic SHA-256 is
`8915ac3e6a21fca55c8386b8affce516f9b29aa3d4fcb00dca48fec0fb8f4eed`.

Canonical evidence uses two official server JVMs and four client sessions.
