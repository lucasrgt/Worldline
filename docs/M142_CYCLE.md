# M142 qualification cycle

`PistonExtensionCycle` compiles the production b1.7.3 adapter with the M142
smoke and rebuilds the complete raised piston fixture in two fresh official
server JVMs. Each run requires the same exact orientation, four-cell raised
delta, fresh-session states and semantic trace.

The frozen semantic SHA-256 is
`48c199a75f4cb6d77ffd1cfec3081c5fa9880915553d5b7e913ddc7cb6a38a20`.

Canonical evidence uses two official server JVMs and four client sessions.
