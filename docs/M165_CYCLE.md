# M165 qualification cycle

`StoneButtonCycle` rebuilds the raised stone plus stone-button fixture in two
fresh official server JVMs. Each run places item 77 on a side face, pulses it
with an empty hand, and reloads the unpowered button.

The frozen semantic SHA-256 is
`293f5014cd6d64c96de7f120544a33fdc68c2ac3843deba132c1d00ee8e00300`.

Canonical evidence uses two official server JVMs and four client sessions.
